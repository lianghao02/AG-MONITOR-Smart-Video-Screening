# 01_AG-MONITOR-Smart-Video-Screening Agent 開發規範

本專案遵循目前有效之全域開發憲法；本檔僅定義專案專屬規則與例外。

---

## 1. 技術棧與選型邊界
- **主力架構**：Python 3.13 + PyAV + OpenCV + Ultralytics YOLO (YOLOv8/11/12) + Eel (HTML/JS/WebSocket)。
- **技術選型邊界**：
  - 專案核心極度依賴 Python 深度學習與影音處理生態系（PyAV 零拷貝解碼、YOLO 推論、ByteTrack 軌跡追蹤）。
  - **嚴禁隨意進行 C#、Rust 或前端 SPA 框架重寫**；主程式（`main.py`）過大問題採「漸進拆分、狀態模型抽離、管線隔離」，而非隨意更換語言。
- **可攜發布模式**：
  - 依賴由本機 `python_embed` 提供 CPU 通用 Runtime；啟動腳本嚴格遵循全域 BAT 薄啟動器原則，邏輯由 PowerShell (`setup_and_run.ps1`) 承載。

---

## 2. 業務領域與快篩鑑識邊界
- **建立「影片級任務狀態模型」**：
  - 批次工作台必須以「單一影片任務」為粒度管理狀態（等待中、解碼中、分析進度百分比、目標偵測統計、完成、失敗原因）。
  - 清楚區分並統計偵測結果（人、單車、機車、汽車、公車、貨車等目標數）。
- **非阻塞式 UI 與推論分離**：
  - PyAV 影音解碼、YOLO AI 推論與非同步截圖寫入（Writer Queue）必須維持背景管線隔離，**嚴禁阻塞 WebSocket 與 Eel UI 主執行緒**。
  - 全速快篩（Headless 批次模式）應停用逐幀畫面回傳，以最大化推論吞吐量。
- **鑑識防禦與截圖完整性**：
  - 截圖採零覆寫命名，強制輸出至 `captures/`，保留原始影像解析度與監視器角落 OSD 資訊；預設嚴禁修改畫面原始像素。
  - 檔案重新命名或整理必須維持獨立交易防護，避免搬移時檔案錯置或遺失。
  - **鑑識聲明**：AI 偵測結果僅供快速篩選與研判輔助，嚴禁宣稱為絕對零漏失，亦不取代原始法規影音證物與承辦人判斷。

---

## 3. 穩定性與防禦規範
- **壞軌與損毀容錯**：
  - 針對非標準監視器裸流（`.h265`, `.dav`, `.264`）與損毀影片，必須實施結構化例外捕捉，標記失敗原因後安全跳過，嚴禁導致整個批次佇列崩潰中斷。
- **安全停止機制**：
  - 正常停止必須排空 Writer Queue 確保截圖寫入完整；強制中斷時才得安全清空佇列。

---

## 4. 核心驗證方式
- 修改影音流程、程序控制或快篩邏輯後，必須執行核心單元測試：
  ```bash
  python -m unittest tests/test_process_context.py
  ```
